# pivot index = sum of all numbers to the left of the index
        x = int(len(nums)/2)
        # left side and right side initialization
        lp = []
        rp = []
        # left side calc: calculate sum at range 0 to len/2
        for i in range(0, x):
            lp[0] += nums[x]
        # right side calc: calculate sum at range len/2 to len

        # return the left most pivot index
        # if the pivot 
        if():
            pass
        # if no index exists
        else:
            return -1
